import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import { Providers } from './providers'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'CodeStream AI - Real-Time Code Review Platform',
  description: 'Distributed real-time code collaboration with AI-powered reviews',
  keywords: ['code review', 'AI', 'real-time collaboration', 'WebSocket', 'distributed systems'],
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className}>
        <Providers>{children}</Providers>
      </body>
    </html>
  )
}
